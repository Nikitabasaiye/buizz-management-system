import { PagePlaceholder } from "@/components/common/PagePlaceholder";

export default function CreateEventPage() {
  return <PagePlaceholder eyebrow="Organizer" title="Create event placeholder" description="Prepared for multi-step event creation with forms, validation, and ticket category setup." />;
}
