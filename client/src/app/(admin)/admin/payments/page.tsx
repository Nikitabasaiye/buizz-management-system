import { PagePlaceholder } from "@/components/common/PagePlaceholder";

export default function AdminPaymentsPage() {
  return <PagePlaceholder eyebrow="Admin" title="Payments placeholder" description="Prepared for transactions, refunds, settlement checks, and payment reconciliation." />;
}
