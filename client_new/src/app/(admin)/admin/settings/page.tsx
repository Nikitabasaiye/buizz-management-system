import { PagePlaceholder } from "@/components/common/PagePlaceholder";

export default function AdminSettingsPage() {
  return <PagePlaceholder eyebrow="Admin" title="Admin settings placeholder" description="Prepared for platform settings, admin permissions, and operational configuration." />;
}
