{
  "name": "buizz-management-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm run dev",
    "build:server": "cd server && npm install",
    "start:server": "cd server && npm start"
  }
}
