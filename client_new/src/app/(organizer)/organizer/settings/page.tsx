import { PagePlaceholder } from "@/components/common/PagePlaceholder";

export default function OrganizerSettingsPage() {
  return <PagePlaceholder eyebrow="Organizer" title="Organizer settings placeholder" description="Prepared for organizer profile, team permissions, and payout configuration." />;
}
